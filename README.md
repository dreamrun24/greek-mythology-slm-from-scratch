# Greek Mythology SLM from Scratch

A GPT-style Small Language Model (~51M parameters) built and trained 
from scratch on classical Greek mythology texts (Homer, Hesiod, Ovid). 
Implements transformer architecture, tokenization, and training loop 
in pure PyTorch.
